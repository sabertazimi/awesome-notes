---
sidebar_position: 30
tags: [Web, Network, Toolchain, Linux]
---

# Toolchain

- `ifconfig` + `egrep`.
- `netstat` + `egrep`.
- `tcpdump`.
- `nslookup`.
- `wireshark`.

```bash
sudo dpkg-reconfigure wireshark-common
sudo gpasswd -a $USER wireshark
```
